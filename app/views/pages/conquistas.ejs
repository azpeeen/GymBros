<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<%- include('../partials/seo') %>
<link rel="icon" type="image/x-icon" href="/images/favicon.ico">
<link rel="stylesheet" href="/css/header.css">
<link rel="stylesheet" href="/css/footer.css">
<link rel="stylesheet" href="/css/area-aluno.css">
<link rel="stylesheet" href="/css/conquistas.css">
</head>
<body>

<%- include('../partials/header') %>

<section class="aluno-dashboard">
    <section class="sidebar-overlay" id="sidebarOverlay"></section>

    <!-- SIDEBAR -->
    <section class="aluno-sidebar">
        <section class="aluno-profile">
            <section class="avatar">
                <img src="<%= user.profile_photo || '/images/avatar.png' %>" alt="Avatar do Aluno">
            </section>
            <h2><%= user.nome %></h2>
            <p data-translate="sidebar.role">Aluno GymBros</p>
        </section>
        <ul class="aluno-menu">
            <li><a href="/area-aluno" data-translate="sidebar.painel"><i class="fas fa-tachometer-alt"></i> Painel</a></li>
            <li><a href="/treinos" data-translate="sidebar.treinos"><i class="fas fa-dumbbell"></i> Meus Treinos</a></li>
            <li><a href="/evolucao" data-translate="sidebar.evolucao"><i class="fas fa-chart-line"></i> Evolução</a></li>
            <% if (user && (user.planoSlug === 'gymbro' || user.planoSlug === 'black')) { %>
            <li><a href="/ai/chat" data-translate="sidebar.ai"><i class="fas fa-robot"></i> Personal Trainer IA</a></li>
            <li><a href="/ai/avaliacao" data-translate="sidebar.avaliacao"><i class="fas fa-camera"></i> Avaliação Corporal</a></li>
            <% } else { %>
            <li><a href="/meu-plano?upgrade=1" class="menu-locked" data-translate="sidebar.ai"><i class="fas fa-robot"></i> Personal Trainer IA 🔒</a></li>
            <li><a href="/meu-plano?upgrade=1" class="menu-locked" data-translate="sidebar.avaliacao"><i class="fas fa-camera"></i> Avaliação Corporal 🔒</a></li>
            <% } %>
            <li><a href="/imc-form" data-translate="sidebar.imc"><i class="fas fa-clipboard-list"></i> Meu Perfil IMC</a></li>
            <li><a href="/perfil/<%= user.id %>"><i class="fas fa-user-circle"></i> Meu Perfil</a></li>
            <li><a href="/conquistas" class="active" data-translate="sidebar.conquistas"><i class="fas fa-trophy"></i> Conquistas</a></li>
            <li><a href="/meu-plano" data-translate="sidebar.plano"><i class="fas fa-file-alt"></i> Meu Plano</a></li>
            <li><a href="/suporte" data-translate="sidebar.suporte"><i class="fas fa-headset"></i> Suporte</a></li>
            <li><a href="/config" data-translate="sidebar.config"><i class="fas fa-cog"></i> Configurações</a></li>
        </ul>
    </section>

    <!-- CONTEÚDO PRINCIPAL -->
    <section class="aluno-main">

        <section class="conquistas-header">
            <h1><i class="fas fa-trophy"></i> Minhas Conquistas</h1>
            <p class="conquistas-subtitle"><%= totalDesbloqueadas %> / <%= total %> conquistadas</p>
            <section class="conquistas-progress-bar">
                <section class="conquistas-progress-fill" style="width: <%= total > 0 ? Math.round((totalDesbloqueadas/total)*100) : 0 %>%"></section>
            </section>
        </section>

        <%
          const categoriaInfo = {
            braco:        { nome: 'Braço',        icone: '💪' },
            perna:        { nome: 'Perna',        icone: '🦵' },
            peito:        { nome: 'Peito',        icone: '🏋️' },
            costas:       { nome: 'Costas',       icone: '🔙' },
            ombro:        { nome: 'Ombro',        icone: '🏔️' },
            core:         { nome: 'Core',         icone: '🔥' },
            cardio:       { nome: 'Cardio',       icone: '🏃' },
            consistencia: { nome: 'Consistência', icone: '🏆' },
            ia:           { nome: 'IA',           icone: '🤖' },
          };
          const categorias = {};
          lista.forEach(c => {
            if (!categorias[c.categoria]) categorias[c.categoria] = [];
            categorias[c.categoria].push(c);
          });
        %>

        <% Object.entries(categorias).forEach(([cat, itens]) => { %>
        <section class="conquistas-categoria">
            <h2 class="conquistas-cat-title">
                <%= categoriaInfo[cat]?.icone || '🏅' %> <%= categoriaInfo[cat]?.nome || cat %>
            </h2>
            <section class="conquistas-grid">
                <% itens.forEach(c => { %>
                <section class="achievement-card <%= c.desbloqueada ? 'unlocked tier-' + c.tier : 'locked' %>">
                    <section class="achievement-icon"><%= c.desbloqueada ? c.icone : '🔒' %></section>
                    <section class="achievement-tier tier-badge-<%= c.desbloqueada ? c.tier : 'locked' %>">
                        <%= c.desbloqueada ? c.tier.toUpperCase() : '???' %>
                    </section>
                    <section class="achievement-name"><%= c.desbloqueada ? c.nome : '???' %></section>
                    <section class="achievement-desc"><%= c.descricao %></section>
                    <% if (c.desbloqueada && c.desbloqueada_em) { %>
                    <section class="achievement-date">
                        <%= new Date(c.desbloqueada_em).toLocaleDateString('pt-BR') %>
                    </section>
                    <% } %>
                </section>
                <% }); %>
            </section>
        </section>
        <% }); %>

    </section>
</section>

<%- include('../partials/footer') %>
<link rel="preload" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"></noscript>

<script>
(function () {
    var btn     = document.getElementById('menuToggle');
    var sidebar = document.querySelector('.aluno-sidebar');
    var overlay = document.getElementById('sidebarOverlay');
    if (!btn || !sidebar || !overlay) return;
    function openMenu()  { sidebar.classList.add('open'); overlay.classList.add('open'); document.body.style.overflow = 'hidden'; }
    function closeMenu() { sidebar.classList.remove('open'); overlay.classList.remove('open'); document.body.style.overflow = ''; }
    btn.addEventListener('click', openMenu);
    overlay.addEventListener('click', closeMenu);
    sidebar.querySelectorAll('a').forEach(function (a) { a.addEventListener('click', closeMenu); });
})();
</script>
</body>
</html>
